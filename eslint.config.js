module.exports = {
    languageOptions: {
        globals: {
            browser: true,
            es2021: true
        }
    },
    rules: {
        "no-unused-vars": "error",
        semi: ["warn", "always"],
        quotes: ["warn", "double"]
    }
};